# goalchain_backend — ARCHIVED

**Status:** Post-Mundial cleanup (2026-05-26)

This package is **not** part of the active GoalChain stack. Transactional API lives in [`goalchain_api/`](../goalchain_api/). Play UI in [`goalchain_webapp/`](../goalchain_webapp/).

Do not add new features here. Prefer deleting `dist/` and `node_modules/` from clones after archival review.
